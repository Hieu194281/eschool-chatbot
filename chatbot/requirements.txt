# Tuyển Sinh Concierge — Pha 1 dependencies (PINNED 2026-07-22, Python 3.12).
# Direct deps pinned to exact versions for reproducible installs.
# Full transitive tree (byte-exact) lives in requirements.lock — use that for
# an identical environment: `pip install -r requirements.lock`.

fastapi==0.139.2
uvicorn[standard]==0.51.0

# LangGraph brain + Postgres checkpointer
langgraph==1.2.9
langgraph-checkpoint-postgres==3.1.0
langchain-core==1.5.0

# LLM provider — swapped via init_chat_model + LLM_MODEL_* env (no code change).
# ACTIVE: Gemini (google_genai) for chat + embeddings. langchain-openai kept for the
# OpenAI-compatible gateway path (ViRouter/OpenRouter) documented in .env.example.
langchain==1.3.14
langchain-openai==1.4.0
langchain-google-genai==4.3.1

# RAG vector math — langchain InMemoryVectorStore cosine similarity needs numpy.
numpy==2.5.1

# Postgres async driver (used by AsyncPostgresSaver + handoff_status table)
psycopg[binary,pool]==3.3.4

# Google Sheets (KB + leads)
gspread==6.2.1

# HTTP (Messenger Send API, Telegram)
httpx==0.28.1

# Config + scheduling
pydantic==2.13.4
pydantic-settings==2.14.2
apscheduler==3.11.3

# Testing
pytest==9.1.1
pytest-asyncio==1.4.0
