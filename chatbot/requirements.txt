# Tuyển Sinh Concierge — Pha 1 dependencies.
# Plan directive: install LATEST STABLE, then `pip freeze > requirements.txt`.
# These lower-bound pins are a known-good floor; regenerate with a real freeze
# on the deploy host after verifying model IDs + AsyncPostgresSaver import.

fastapi>=0.115
uvicorn[standard]>=0.32

# LangGraph brain + Postgres checkpointer
langgraph>=0.2.60
langgraph-checkpoint-postgres>=2.0.0
langchain-core>=0.3.30
langchain-google-genai>=2.0.0
google-genai>=0.3.0

# Postgres async driver (used by AsyncPostgresSaver + handoff_status table)
psycopg[binary,pool]>=3.2

# Google Sheets (KB + leads)
gspread>=6.1

# HTTP (Messenger Send API, Telegram)
httpx>=0.27

# Config + scheduling
pydantic>=2.9
pydantic-settings>=2.6
apscheduler>=3.10

# Testing
pytest>=8.3
pytest-asyncio>=0.24
